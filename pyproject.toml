[tool.ruff]
line-length = 100
target-version = "py312"
exclude = ["*/migrations/*", "venv"]

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "F",   # pyflakes
    "W",   # pycodestyle warnings
    "I",   # isort
    "DJ",  # flake8-django
    "UP",  # pyupgrade
]
